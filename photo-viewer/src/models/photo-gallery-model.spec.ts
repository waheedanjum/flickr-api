import { PhotoGalleryModel } from './photo-gallery-model';

describe('PhotoGalleryModel', () => {
  it('should create an instance', () => {
    expect(new PhotoGalleryModel()).toBeTruthy();
  });
});
